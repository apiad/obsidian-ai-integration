{
  "name": "obsidian-ai-integration",
  "version": "0.3.1",
  "description": "Obsidian plugin for per-bubble Claude invocation via [!for-claude] callouts",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production && node scripts/deploy-to-vault.mjs",
    "deploy": "node scripts/deploy-to-vault.mjs",
    "verify:install": "node scripts/deploy-to-vault.mjs verify",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "Alejandro Piad",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.7",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
